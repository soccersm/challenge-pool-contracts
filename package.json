{
  "name": "challenge-pool-contracts",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "hardhat": "^2.22.17"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:paymaster": "npx hardhat ignition deploy ignition/modules/AirdropPaymaster.ts --network",
    "verify:paymaster": "npx hardhat ignition deploy ignition/modules/AirdropPaymaster.ts --verify --network",
    "deploy:soccersm": "npx hardhat ignition deploy ignition/modules/Soccersm.ts --network",
    "verify:soccersm": "npx hardhat ignition deploy ignition/modules/Soccersm.ts --verify --network"
  }
}
